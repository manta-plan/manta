# Test stack for the backend's integration suite (see
# backend/tests/integration/conftest.py). Includes every service from
# compose-dev-services.yaml wholesale — this file only adds test-specific
# isolation (its own Compose project name) on top. Host ports are randomized
# via docker/.env.test, not by overriding anything here.
name: manta-test

include:
  - compose-dev-services.yaml
